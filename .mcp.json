{
  "mcpServers": {
    "sermonflow-slides": {
      "command": "uv",
      "args": ["run", "--quiet", "--project", "${CLAUDE_PLUGIN_ROOT}", "sermonflow-mcp"],
      "type": "stdio",
      "disabled": false,
      "env": {}
    }
  }
}
